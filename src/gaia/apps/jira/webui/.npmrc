# GAIA JIRA Dashboard npm configuration

# Use exact versions to ensure consistency
save-exact=true

# Set registry (use default npm registry)
registry=https://registry.npmjs.org/

# Enable package-lock for reproducible builds
package-lock=true

# Disable fund messages
fund=false

# Set npm log level
loglevel=warn